import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jogodavelha.app',
  appName: 'JogoDaVelhaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
